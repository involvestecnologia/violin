---
name: Icon
menu: Components
route: /components/Icon
---

import { Icon } from './Icon';
import { Playground, Props } from "docz";

# Icon

> This requires import [Material Icons font](https://google.github.io/material-design-icons/#icon-font-for-the-web) to your project or use GlobalStyle component to do this.

<Playground>
    <Icon hasIcon="favorite" hasSize={50} hasColor="default" />
    <Icon hasIcon="directions_bike" hasSize={50} hasColor="accent" />
    <Icon hasIcon="sports_esports" hasSize={50} hasColor="primary" onClick={() => alert("Clicked!")} />
</Playground>

## Props

<Props of={Icon} />